$("#comments-<%= @review.id %>").html(
    "<%= escape_javascript(render @comments) %>"
);

$("#comment-form-<%= @review.id %>").html(
    "<%= escape_javascript(render 'comments/form', review: @review) %>"
);
