<%= provide :title, "#{@user.username}さんの行きつけ" %>
<div class="container">
  <div class="index">
    <h2><strong><%= link_to @user.username, @user %>さんの行きつけ</strong></h2>
    <hr>
    <div class="row">
      <%= render @parlors %>
    </div>
  </div>
</div>
